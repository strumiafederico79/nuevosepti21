fastapi
uvicorn[standard]
python-multipart
pydantic
slowapi>=0.1.10
numpy
scipy
librosa
soundfile
pydub>=0.25.1  # fallback MP3 si ffmpeg no esta disponible en el sistema
# Python 3.13 eliminó el módulo "audioop" de la stdlib (PEP 594) y pydub lo
# necesita internamente. audioop-lts lo reemplaza con el mismo API.
audioop-lts>=0.2.2; python_version>="3.13"
psutil
pyloudnorm
websockets
numba
python-dotenv
noisereduce

# IA (Laia) - Gemini
# Usa httpx para llamadas HTTP async a la API de Gemini
httpx>=0.28.0
requests
google-generativeai

# Separacion de stems (Demucs)
# NOTA (migracion Ubuntu / Python 3.13+):
# El paquete "demucs" original de Meta (facebookresearch) dejo de mantenerse
# y no instala en pip con Python 3.10+. Usamos "demucs-infer", un fork
# mantenido, con el mismo API publico (demucs_infer.pretrained.get_model,
# demucs_infer.apply.apply_model) y compatible con PyTorch 2.x.
# torch/torchaudio ya no necesitan pinearse a 1.13.1 (eso era por Windows 7 /
# Python 3.8); en Ubuntu + Python 3.13 se usan las versiones actuales.
torch>=2.6
torchaudio>=2.6
demucs-infer>=4.1.2

# Separacion voz/instrumental de alta calidad (modo alternativo a Demucs)
# via BS-RoFormer / Mel-Band RoFormer. Usa el mismo wrapper que UVR.
# Instalar variante GPU si hay CUDA disponible: audio-separator[gpu]
audio-separator[cpu]
